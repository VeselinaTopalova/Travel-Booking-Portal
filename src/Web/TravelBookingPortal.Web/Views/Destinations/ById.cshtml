@model TravelBookingPortal.Web.ViewModels.Destinations.DestinationViewModel

<section class="py-5">
    <div class="container">

        <div>            
            <div>
                <h5 class="mt-0">@Model.Name Tours and Trips Packages 2020/2021</h5>
            </div>
            <p>&nbsp;</p>
            <div class="row">
                @foreach (var tour in Model.Tours)
                {
                    <div class="col-md-4 media">
                        <div class="card">
                            <img src="@tour.Image" class="card-img-top" alt="@tour.Name">
                            <div class="card-body">
                                <h5 class="card-title">@tour.Name</h5>
                                <p class="card-text">@tour.Duration days</p>
                                <p class="card-text">From US$@tour.PriceTotal</p>
                                <h6 class="media-heading"><a asp-controller="Tours" asp-action="ById" asp-route-id="@tour.Id">LEARN MORE</a></h6>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</section>
